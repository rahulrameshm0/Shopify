services:
  web:
    build: .
    ports: 
      - "8000:8000"

  db:
    image: postgres:17
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
volumes:
  postgres_data: